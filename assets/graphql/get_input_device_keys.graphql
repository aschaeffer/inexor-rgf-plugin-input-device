query keys {
  instances {
    keys: entities(entityType: "input_device_key") {
      id
      properties(
        names: [
          "label"
          "name"
          "key"
          "key_code"
          "key_down"
        ]
      )
    }
  }
}
