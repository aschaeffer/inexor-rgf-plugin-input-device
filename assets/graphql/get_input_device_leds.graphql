query leds {
  instances {
    leds: entities(entityType: "input_device_led") {
      id
      properties(
        names: [
          "label"
          "name"
          "led"
          "led_type"
          "state"
        ]
      )
    }
  }
}
