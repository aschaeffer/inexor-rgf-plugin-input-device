query absolute_axes {
  instances {
    absolute_axes: entities(entityType: "input_device_absolute_axis") {
      id
      properties(
        names: [
          "label"
          "name"
          "absolute_axis"
          "absolute_axis_type"
          "state"
        ]
      )
    }
  }
}
